import ChatItem from '../ChatItem/ChatItem';
import './ChatList.css';

const ChatList = () => {
    return (
        <div className="chat-list">
            <ChatItem />
        </div>
    );
}

export default ChatList;